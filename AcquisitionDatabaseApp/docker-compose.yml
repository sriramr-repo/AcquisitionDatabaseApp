services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scm
      POSTGRES_PASSWORD: scm
      POSTGRES_DB: scm_dashboard
    ports: ["5432:5432"]
    volumes: ["scm-dashboard-postgres:/var/lib/postgresql/data"]
volumes:
  scm-dashboard-postgres:
